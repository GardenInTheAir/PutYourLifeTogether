type User @table {
  displayName: String!
  createdAt: Timestamp!
  email: String
  photoUrl: String
}

type Category @table {
  name: String!
  createdAt: Timestamp!
  description: String
  user: User!
}

type Task @table {
  title: String!
  createdAt: Timestamp!
  isCompleted: Boolean!
  description: String
  dueDate: Date
  user: User!
  category: Category
}